# STM32 Hardware abstraction and CMSIS layer

stm32_cube_dir = meson.current_source_dir() / 'STM32CubeF4'

cmsis_dir =  stm32_cube_dir / 'Drivers' / 'CMSIS'
hal_dir = stm32_cube_dir / 'Drivers' / 'STM32F4xx_HAL_DRIVER'

message(cmsis_dir)
message(hal_dir)
